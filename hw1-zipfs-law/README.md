# HW1 — Corpus Statistics

- `hw1-2.py` — tokenizes a dialogue corpus (`train.jsonl`, not included) with NLTK and checks
  whether word-frequency rank follows Zipf's law; produces `zipf_law_graph.png`.
- `hw1-4.py` — builds an n-gram language model over the NLTK Brown corpus (sentence
  preprocessing, `<s>`/`</s>` markers, smoothing) and evaluates it with perplexity.

Full write-up: [`report.pdf`](./report.pdf).
